<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :cust_last %>
    <%= f.text_field :cust_last %>
  </div>

  <div class="field">
    <%= f.label :cust_first %>
    <%= f.text_field :cust_first %>
  </div>

  <div class="field">
    <%= f.label :cust_email %>
    <%= f.text_field :cust_email %>
  </div>

  <div class="field">
    <%= f.label :showing_id %>
    <%= f.text_field :showing_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
